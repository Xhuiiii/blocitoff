<%= content_tag :li, class: 'list-group-item todo-item-even todo-item-odd', id: "item-#{item.id}" do %>
  <%= item.name %>
  <%= link_to " ", [current_user, item],format: :js, method: :delete, class: 'glyphicon glyphicon-ok pull-right', remote: true %>
<% end %>